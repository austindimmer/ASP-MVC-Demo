@model IEnumerable<Powerfront.Backend.Impact.EntityFramework.BeneficiaryGroup>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.BeneficiaryGroupDescription)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.BeneficiaryGroupDescription)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.BeneficiaryGroupId }) |
            @Html.ActionLink("Details", "Details", new { id=item.BeneficiaryGroupId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.BeneficiaryGroupId })
        </td>
    </tr>
}

</table>
